import { useState } from 'react';import { useProjectStore } from '../../state/projectStore';import { useEditorStore } from '../../state/editorStore';import { exportProjectFile } from '../../services/export/projectExporter';import { downloadNavigation,downloadPaths,downloadWaypoints,downloadZones } from '../../services/export/navigationExporters';import { exportPgm,exportRosZip,generateRosYaml } from '../../services/export/rosExporter';import { downloadBlob,downloadTextFile } from '../../utils/files';
export function ExportDialog({onClose}:{onClose:()=>void}){const p=useProjectStore(),selection=useEditorStore(s=>s.selection);const [busy,setBusy]=useState(false),[error,setError]=useState('');const selectedObjects=p.objects.filter(x=>selection.includes(x.id)),selectedPaths=p.paths.filter(x=>selection.includes(x.id)),selectedZones=p.zones.filter(x=>selection.includes(x.id));const run=async(type:string,selected=false)=>{setError('');setBusy(true);try{const os=selected?selectedObjects:p.objects,ps=selected?selectedPaths:p.paths,zs=selected?selectedZones:p.zones;if(type==='project')exportProjectFile(p.toProjectFile());else if(type==='waypoints')downloadWaypoints(os);else if(type==='paths')downloadPaths(ps);else if(type==='zones')downloadZones(zs);else if(type==='navigation')downloadNavigation(p.metadata,os,ps,zs,[p.robot],`${p.name.replace(/[^a-z0-9-_]+/gi,'-').replace(/^-+|-+$/g,'').toLowerCase()||'map'}-navigation.json`);else{if(!p.image)throw new Error('No occupancy map image is loaded.');if(type==='ros')await exportRosZip(p.metadata,p.image,p.name);else if(type==='pgm')await exportPgm(p.metadata,p.image);else if(type==='yaml')downloadTextFile(generateRosYaml(p.metadata,p.image),'map.yaml','application/yaml');else if(type==='png'){const blob=await (await fetch(p.image.dataUrl)).blob();downloadBlob(blob,'occupancy.png')}}}catch(e){setError(e instanceof Error?e.message:String(e))}finally{setBusy(false)}};return <div className="modalback"><div className="modal exportmodal"><h3>Export Map</h3>{error&&<div className="errorbox">{error}</div>}<div className="exportgrid">{[['project','AMR Project'],['ros','ROS Map (.zip)'],['png','Occupancy PNG'],['pgm','PGM'],['yaml','YAML'],['waypoints','Waypoints JSON'],['paths','Paths JSON'],['zones','Zones JSON'],['navigation','Navigation JSON (Waypoints + Paths + Zones)']].map(([id,label])=><div className="exportrow" key={id}><b>{label}</b><button disabled={busy} onClick={()=>void run(id,false)}>Export All</button>{['waypoints','paths','zones','navigation'].includes(id)&&<button disabled={busy||!selection.length} onClick={()=>void run(id,true)}>Export Selected</button>}</div>)}</div><div className="modalactions"><button onClick={onClose} disabled={busy}>Close</button></div></div></div>}
