import { describe,expect,it } from 'vitest';import { migrateProject } from '../schemas/projectSchema';
const base={format:'AMR_MAP_PROJECT',version:'1.0',project:{id:'p',name:'P',metadata:{id:'m',name:'M',width:10,height:10,resolution:.05,originX:0,originY:0,originYaw:0},image:null,objects:[],paths:[],zones:[],robotConfigs:[{name:'r',width:.7,length:1,footprint:{type:'rectangle',width:.7,length:1},safetyMargin:.1,inflationRadius:.2,minimumClearance:.1,minimumTurningRadius:.5,maxSpeed:1}],createdAt:new Date().toISOString(),updatedAt:new Date().toISOString(),revision:1}};
describe('project schema',()=>{it('accepts 1.0',()=>expect(migrateProject(base).project.id).toBe('p'));it('rejects unsupported version',()=>expect(()=>migrateProject({...base,version:'2.0'})).toThrow(/unsupported/))});
